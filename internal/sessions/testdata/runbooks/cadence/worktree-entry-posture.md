# Worktree entry posture

Feature work in a branch-mode repo starts in a worktree: branch, push -u,
draft PR at entry. The enforce-worktree guard blocks commits in the primary
checkout regardless of branch.
