# Binaries
/forgectl
/fx
/dist/
# Staged bless helper — the release workflow builds it here for goreleaser's
# darwin archive to pick up.
/dist-helper/

# Go
*.test
*.out
coverage.*

# env set's flock sibling — created next to whatever .env file was locked
*.env.lock

# Swift (bless ceremony helper)
helper/forgectl-bless-helper/.build/

# Editor / OS
.DS_Store

# Cadence / subagent session-local scratch
.claude/intros/
.claude/agent-memory/
.claude/test-output/
